<html>

<head>
  <title>{{obj.name}}</title>
</head>

<body>
  {{ now() }}
  <h1>{{obj.name}}</h1>
  The entries in the obj:<br>
  <a href="add">add more</a><br><br>
  {% var idx = 0 %} {# TODO for is not that advanced yet; workaround #}
  {% for entry in obj.entries %} {# for idx, entry in obj.entries.pair() does not work yet. see above #}
    {#{% include entry.ninja %}#}
    <a href="{{ctx.urlFor("entry", [("id", $idx)] ) }}">Num: {{idx}} ({{entry.ss}})</a><br>
    {% idx.inc %}
  {% endfor %}
</body>

</html>